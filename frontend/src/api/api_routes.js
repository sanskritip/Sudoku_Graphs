const host = "https://guarded-reef-63269.herokuapp.com/api/"
//const host = "http://localhost:8000/api/"

const routes = {
    test:host+"test",
    generate:host+"generate",
    solve:host+'solution'
}

export default routes